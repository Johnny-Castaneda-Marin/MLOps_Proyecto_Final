apiVersion: v1
kind: Service
metadata:
  name: streamlit-service
  namespace: mlops-ui
  labels:
    app: streamlit
spec:
  selector:
    app: streamlit
  ports:
    - port: 8501
      targetPort: 8501
      protocol: TCP
  type: ClusterIP
